input
    last hit status?

memory
    shot list
    hit, but not sunk?(save unsunk hits? schwer für edgecases, aka touching ships)  TODO

actions
    fire random until hit (never twice the same)
    hit adjacent squares (dumb yep)until sunk  
    hit adjacent squares (smart)until sunk  TODO
    return to random                        TODO